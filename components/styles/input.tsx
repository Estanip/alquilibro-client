import { StyleSheet } from "react-native";

export const input = StyleSheet.create({

    container: {
        alignSelf: 'center',
        padding: 10,
        width: '90%',
        height: '5%',
        backgroundColor: '#ECEFF0',
        marginBottom: 10
    }

});
